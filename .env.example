# Canonical origin. Every canonical URL, hreflang alternate, sitemap entry and
# JSON-LD @id is built from this. Set it to the production domain before the
# site is indexed — changing it later means a full re-index.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
